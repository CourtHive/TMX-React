import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles(() => ({
  splash: {
    width: '100%',
    display: 'flex',
    marginTop: '2em',
    paddingTop: '5em',
    flexDirection: 'row',
    justifyContent: 'center',
    alignContent: 'center',
    alignItems: 'center'
  }
}));
