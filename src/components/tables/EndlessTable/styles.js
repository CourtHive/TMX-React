import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles(() => ({
  tableRow: {
    opacity: '1!important',
    padding: '0!important'
  },
  tableCell: {
    paddingBottom: '0',
    paddingTop: '0'
  }
}));
